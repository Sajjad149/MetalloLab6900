# راه‌اندازی MetalloLab روی GitHub و ساخت APK

## ۱) ساخت مخزن
1. وارد https://github.com شوید و حساب بسازید یا وارد شوید.
2. روی علامت `+` و سپس `New repository` بزنید.
3. نام مخزن را `MetalloLab` بگذارید.
4. گزینه Public یا Private را انتخاب کنید. برای شروع Private مناسب‌تر است.
5. تیک Add a README را نزنید؛ سورس پروژه خودش فایل‌های لازم را دارد.
6. روی Create repository بزنید.

## ۲) بارگذاری سورس از مرورگر گوشی یا کامپیوتر
1. ZIP سورس را روی دستگاه دریافت و استخراج کنید.
2. در صفحه مخزن GitHub، گزینه `Add file` و سپس `Upload files` را بزنید.
3. تمام محتویات پوشه پروژه را انتخاب و بارگذاری کنید؛ خود پوشه مادر MetalloLab را به‌جای محتویات آن آپلود نکنید.
4. مهم: فایل مخفی `.github/workflows/android.yml` نیز باید در مخزن باشد. اگر انتخاب فایل‌های مخفی در گوشی دشوار است، از بخش ۳ استفاده کنید.
5. پایین صفحه در بخش Commit changes، پیام `Add MetalloLab source` را وارد و Commit کنید.

## ۳) روش مطمئن برای فایل workflow
پس از آپلود بقیه فایل‌ها:
1. در صفحه مخزن روی `Add file` > `Create new file` بزنید.
2. در نام فایل دقیقاً وارد کنید:
   `.github/workflows/android.yml`
3. محتوای فایل workflow را از پوشه `.github/workflows/android.yml` در ZIP باز و کپی کنید.
4. Commit changes را بزنید.

## ۴) اجرای ساخت APK
1. در مخزن به تب `Actions` بروید.
2. اگر GitHub درخواست فعال‌سازی Actions داد، گزینه فعال‌سازی را بزنید.
3. در فهرست سمت چپ workflow با نام `Build MetalloLab Android APK` را انتخاب کنید.
4. روی `Run workflow` بزنید و شاخه `main` را انتخاب کنید.
5. دکمه سبز `Run workflow` را بزنید.
6. اجرای جدید در فهرست ظاهر می‌شود. روی آن بزنید و منتظر سبز شدن همه مراحل بمانید. بار اول ممکن است چند دقیقه طول بکشد.

## ۵) دانلود APK
1. در صفحه اجرای موفق، به قسمت `Artifacts` در پایین صفحه بروید.
2. روی `MetalloLab-debug-arm64` بزنید تا ZIP حاوی APK دانلود شود.
3. آن ZIP را استخراج کنید؛ فایل `app-debug.apk` داخل آن است.
4. APK را به گوشی اندرویدی منتقل و نصب کنید. ممکن است اندروید برای نصب از منبع ناشناس اجازه بخواهد؛ فقط اگر به سورس و فایل ساخته‌شده اعتماد دارید، اجازه بدهید.

## ۶) ساخت نسخه جدید
هر بار کد را تغییر بده و commit/push به شاخه `main` کن؛ workflow به‌صورت خودکار اجرا می‌شود. همچنین می‌توانی هر زمان از Actions، گزینه Run workflow را اجرا کنی.

## نکات و عیب‌یابی
- این workflow یک APK دیباگ می‌سازد که برای آزمایش شخصی مناسب است، نه انتشار نهایی در فروشگاه.
- برای انتشار عمومی باید نسخه Release را با کلید امضای خصوصی خودت امضا کنی. کلید و رمز آن را در سورس عمومی قرار نده؛ از GitHub Actions Secrets و پیکربندی امن signing استفاده کن.
- پروژه از `arm64-v8a` فیلتر شده است. این معماری روی اکثر گوشی‌های اندرویدی ۶۴ بیتی ARM اجرا می‌شود، اما روی دستگاه‌های صرفاً ۳۲ بیتی نصب نخواهد شد.
- اگر workflow به دلیل SDK یا نسخه Gradle خطا داد، متن خطای مرحله قرمز را از Actions کپی کن تا تنظیمات دقیق اصلاح شوند.
- فایل‌های سورس را در ریشه مخزن قرار بده؛ در ریشه باید `settings.gradle.kts` و `app/` دیده شوند.
